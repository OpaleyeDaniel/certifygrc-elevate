/**
 * DotGrid — thin re-export of DotGridTexture (the canonical dotted-card
 * texture component). Kept so any import of `DotGrid` still resolves;
 * prefer importing `DotGridTexture` directly for new code.
 */
export { default } from "@/components/visual/DotGridTexture";
